import styled from 'styled-components';

const ListedProfessor = styled.h1`
    color: white;
    @media (orientation: landscape) {
        font-size: 1.7rem;
        padding-left: 5vmin;
        padding-right: 5vmin;
    }
`;

export default ListedProfessor;